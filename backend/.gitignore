# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/

# Environment + secrets
.env

# Data (uploaded audio, generated docs, model caches, DB file)
data/*.db
data/*.db-journal
data/uploads/
data/outputs/
data/models/
# User-uploaded export templates are runtime data like the uploads above -
# the repo carries code only, so a tester's own .docx never lands in a commit.
data/templates/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Logs
*.log
